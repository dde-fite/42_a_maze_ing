from visualizer.src.maze_visualizer import MazeVisualizer

# class MazeGenerator:
#     def generate:
#     def generate_return() -> Maze:


if __name__ == "__main__":
    MazeVisualizer.show_visualizer()
    # m = Mlx()
    # mlx_ptr = m.mlx_init()
    # win_ptr = m.mlx_new_window(mlx_ptr, 1920, 1080, "Test")
    # img_ptr = m.mlx_xpm_file_to_image(mlx_ptr, "visualizer/src/sprites/background.xpm")[0]
    # m.mlx_put_image_to_window(mlx_ptr, win_ptr, img_ptr, 0, 0)
    # m.mlx_loop(mlx_ptr)
