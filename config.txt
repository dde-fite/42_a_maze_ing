# Spaces in key=value lines are not allowed
# Comments in same line as key=value lines are not allowed

# Width of the maze: int
WIDTH=1000

# Height of the maze: int
HEIGHT=1000

# Entry point coordinates of the maze: (int, int)
ENTRY=1,1

# Exit point coordinates of the maze: (int, int)
EXIT=10,10

# Output file name: str finished with .txt
OUTPUT_FILE=maze.txt

# Perfect maze: bool
PERFECT=False

# Seed used, 0 will be treat as random: int
SEED=0

# Ft logo appearing in maze: bool
FT_LOGO=True

# Path finder status (not recommended for more that 10000 cells): bool
PATH_FINDER=False
